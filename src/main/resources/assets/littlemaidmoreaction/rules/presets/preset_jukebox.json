{
  "id": -1,
  "name": "jukebox任务",
  "enabled": false,
  "eventId": "maid_tick",
  "chance": 1.0,
  "cooldown": 10,
  "priority": 100,
  "matchMode": "ALL",
  "conditions": [
    { "key": "task_active", "params": { "task_type": "jukebox", "task_id": "{{task_id}}" } }
  ],
  "actions": [
    {
      "type": "jukebox_interact",
      "params": {
        "target": "{{target}}",
        "target_count": "{{target_count}}"
      }
    }
  ],
  "compat": []
}
