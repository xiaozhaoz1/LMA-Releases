{
  "id": -1,
  "name": "合成任务",
  "enabled": false,
  "eventId": "maid_tick",
  "chance": 1.0,
  "cooldown": 10,
  "priority": 100,
  "matchMode": "ALL",
  "conditions": [
    { "key": "task_active", "params": { "task_type": "craft_chain", "task_id": "{{task_id}}" } }
  ],
  "actions": [
    {
      "type": "craft_chain",
      "params": {
        "target": "{{target}}",
        "target_count": "{{target_count}}"
      }
    }
  ],
  "compat": []
}
