{
  "id": -1,
  "name": "bell_ring任务",
  "enabled": false,
  "eventId": "maid_tick",
  "chance": 1.0,
  "cooldown": 10,
  "priority": 100,
  "matchMode": "ALL",
  "conditions": [
    { "key": "task_active", "params": { "task_type": "bell_ring", "task_id": "{{task_id}}" } }
  ],
  "actions": [
    {
      "type": "bell_ring",
      "params": {
        "target": "{{target}}",
        "target_count": "{{target_count}}"
      }
    }
  ],
  "compat": []
}
